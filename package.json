{
  "name": "pdfcheap",
  "private": true,
  "version": "0.1.0",
  "description": "PDF.cheap — cross-platform PDF editor for macOS, Windows, Linux and Web (PDF Expert-class).",
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @pdfcheap/desktop dev",
    "dev:web": "pnpm --filter @pdfcheap/desktop dev:web",
    "build": "turbo run build",
    "build:web": "pnpm --filter @pdfcheap/desktop build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,css,json,md}\"",
    "tauri": "pnpm --filter @pdfcheap/desktop tauri",
    "desktop": "pnpm --filter @pdfcheap/desktop",
    "converter": "cargo run --manifest-path services/converter/Cargo.toml",
    "icons": "pnpm --filter @pdfcheap/desktop tauri icon app-icon.png"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
