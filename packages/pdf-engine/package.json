{
  "name": "@pdfcheap/pdf-engine",
  "version": "0.1.0",
  "description": "Engine-agnostic PDF abstraction. Swap the open-source PDF.js implementation for a commercial SDK (Apryse/Foxit) behind one interface.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --emitDeclarationOnly --outDir dist || true"
  },
  "dependencies": {
    "pdfjs-dist": "^4.10.38"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
